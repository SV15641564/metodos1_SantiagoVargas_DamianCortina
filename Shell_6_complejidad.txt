La complejidad para el algoritmo del ejercicio anterior puede calcularse mediante r-1, debido a que el valor de r determinará la cantidad de factores restantes al simplificar la fracción. El número de operaciones entonces, viene dado por r-1 debido a que el producto entre dos números cuenta como una multiplicación, es decir, una menos que la cantidad de factores. De ahí que sea r-1.